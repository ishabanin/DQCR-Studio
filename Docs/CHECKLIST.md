# DQCR Studio — Чеклист реализации

**Документ:** `CHECKLIST.md`  
**Версия:** 1.0  
**Дата:** Март 2026

> Использование: отметить `[x]` при завершении, указать дату и ответственного.  
> Каждый раздел соответствует экрану или компоненту. Проверка выполняется тестировщиком + разработчиком.

---

## 🏗️ Phase 0: Foundation

### Инфраструктура

- [ ] Docker Compose запускается командой `docker compose up` без ошибок
- [ ] Frontend доступен по `http://localhost:80`
- [ ] Backend API отвечает по `http://localhost:8000/api/v1/`
- [ ] `GET /health` возвращает `{"status": "ok"}`
- [ ] `GET /ready` возвращает `{"status": "ready"}` только когда FW загружен
- [ ] Nginx корректно проксирует `/api/` → backend и `/ws/` → WebSocket
- [ ] HTTPS работает на staging стенде (self-signed или Let's Encrypt)
- [ ] Все env vars проверяются при старте — приложение падает с понятным сообщением при отсутствии обязательных
- [ ] Logs в JSON формате, уровень INFO по умолчанию
- [ ] GitHub Actions: lint и tests проходят на PR

### Backend Scaffold

- [ ] FastAPI app стартует без ошибок
- [ ] CORS настроен (только домен приложения, не `*`)
- [ ] Path traversal prevention: `../` в file path → 400 Bad Request
- [ ] FWService корректно загружает тестовый DQCR-проект RF110NEW
- [ ] Все Pydantic schemas покрывают обязательные поля из DQCR документации
- [ ] FW-исключения возвращают HTTP 400/422/500, а не 500 с traceback

### Frontend Scaffold

- [ ] Приложение собирается без TypeScript ошибок (`tsc --noEmit`)
- [ ] ESLint не выдаёт ошибок
- [ ] Zustand stores инициализируются корректно
- [ ] React Query client настроен (base URL, auth header)
- [ ] CSS variables определены для всех цветов (light + dark)
- [ ] Dark theme переключается через ThemeProvider
- [ ] ErrorBoundary перехватывает crash и показывает fallback UI

---

## 🧭 Layout & Navigation

### TopBar

- [ ] Логотип отображается, клик ведёт на главную страницу
- [ ] Кнопка `Build ▶` активна только при выбранном проекте
- [ ] Кнопка `Validate` активна только при выбранном проекте
- [ ] Переключатель проекта: выпадающий список загружается из API
- [ ] Смена проекта очищает состояние (открытые файлы, валидация)
- [ ] Переключатель контекста: список контекстов берётся из `contexts/` выбранного проекта
- [ ] Активный контекст визуально выделен (зелёный bg)
- [ ] Смена контекста обновляет StatusBar и данные линейдж-графа

### Sidebar

- [ ] Дерево файлов отображает структуру DQCR-проекта
- [ ] Группировка: Models, Contexts, Parameters, Templates (только admin)
- [ ] Открытие файла через клик добавляет вкладку в редакторе
- [ ] Активный файл подсвечивается (левая зелёная полоса)
- [ ] Правый клик → контекстное меню с пунктами: Открыть, Переименовать, Удалить
- [ ] Переименование работает, имя обновляется в дереве без перезагрузки
- [ ] Удаление запрашивает подтверждение перед выполнением
- [ ] Sidebar корректно отображается при разрешении 1280px

### Tab Bar

- [ ] 6 вкладок: Lineage, Model editor, SQL editor, Validate, Parameters, Build
- [ ] Активная вкладка визуально выделена (зелёная нижняя линия)
- [ ] Горячие клавиши Ctrl+1..6 переключают вкладки
- [ ] Состояние каждого экрана сохраняется при переключении вкладок

### StatusBar

- [ ] Отображает: имя проекта, активный контекст, шаблон, Python версию, FW версию
- [ ] Статус валидации: зелёный ✓ / жёлтый ⚠ N warn / красный ✗ N errors
- [ ] Клик на статус валидации переключает на вкладку Validate
- [ ] Все элементы обновляются при смене проекта / контекста

---

## ⬡ Экран Lineage

### Базовый рендеринг

- [ ] DAG-граф корректно отображается при открытии модели
- [ ] Все папки из `workflow.folders` отрисованы как узлы
- [ ] SQL-файлы отображены как чипы внутри узлов
- [ ] Стрелки зависимостей направлены в правильную сторону
- [ ] Зависимости из `dependency_resolver` корректно отражены (naming_convention / explicit)
- [ ] Граф помещается на экран (auto-fit при загрузке)

### Цветовое кодирование

- [ ] Активная модель: зелёная рамка узла
- [ ] Остальные модели: серая рамка
- [ ] Разрешённые зависимости: серая стрелка
- [ ] Предупреждение: жёлтая стрелка (при конфликте)
- [ ] Ошибка зависимости: красная стрелка (цикл / недостижимый узел)

### Интерактивность

- [ ] Клик на узел папки открывает DetailPanel справа
- [ ] DetailPanel показывает: имя, материализацию, описание, список SQL, зависимости
- [ ] Клик на SQL-чип в DetailPanel открывает файл в SQL Editor
- [ ] Кнопка "Open model ↗" переключает на Model Editor для этой модели
- [ ] Zoom in / zoom out работает (колёсико мыши и кнопки)
- [ ] Fit-to-screen кнопка корректно вписывает граф
- [ ] Контекстный фильтр: папки с `enabled: false` для активного контекста скрыты

### Summary и режимы

- [ ] Бейджи "N folders", "N queries", "N params" корректны
- [ ] Переключение горизонтальный / вертикальный / компактный режим работает
- [ ] Export PNG создаёт читаемое изображение с правильными размерами

---

## ⊞ Экран Model Editor

### Визуальная форма

- [ ] Target Table: поля Name, Schema, Description загружены из model.yml
- [ ] Template dropdown: список из FW TemplateRegistry, текущий шаблон выбран
- [ ] Workflow Engine dropdown: список из FW EngineRegistry
- [ ] Attributes Table: все атрибуты из `target_table.attributes` отображены
- [ ] Атрибут: domain_type отображается как badge с корректным типом
- [ ] Атрибут: флаги is_key и required отображаются как цветные бейджи
- [ ] Атрибут: default_value отображается в колонке Default
- [ ] Кнопка "＋ Add attribute" добавляет пустую строку в таблицу
- [ ] Drag-and-drop атрибутов: строки перетаскиваются, порядок сохраняется
- [ ] Workflow Folders: все папки отображены
- [ ] Папка: materialization dropdown содержит валидные опции из шаблона
- [ ] Папка: enabled toggle работает корректно
- [ ] Кнопка "＋ Add folder" открывает диалог
- [ ] CTE Settings: default и by_context значения загружены и редактируемы

### Bidirectional Sync

- [ ] Изменение поля Name в форме обновляет YAML за < 300 мс
- [ ] Изменение Schema обновляет `target_table.schema` в YAML
- [ ] Добавление атрибута в форме → добавляет элемент в `attributes:` YAML
- [ ] Удаление атрибута → удаляет из YAML
- [ ] Изменение YAML (materialization папки) → форма обновляется
- [ ] SyncBadge показывает "⟳ synced" после завершения обновления
- [ ] SyncBadge показывает "⚠ conflict" при невалидном YAML
- [ ] Переключение Visual → YAML: Monaco открывается с актуальным YAML
- [ ] Переключение YAML → Visual: форма обновляется из YAML (если валиден)
- [ ] Переключение YAML → Visual при conflict: заблокировано с подсказкой
- [ ] Monaco в YAML-режиме показывает подсветку ошибок при невалидной схеме

### Сохранение

- [ ] Кнопка Save сохраняет model.yml на диск
- [ ] После сохранения файл на диске соответствует содержимому YAML
- [ ] Несохранённые изменения индицируются (dirty state)

---

## { } Экран SQL Editor

### Редактор

- [ ] Открытый SQL-файл отображается с корректным содержимым
- [ ] @config блок подсвечивается иным цветом (зеленоватый фон)
- [ ] `{{ macro() }}` вызовы подсвечены синим
- [ ] SQL ключевые слова подсвечены бирюзовым
- [ ] Параметры `{{ date_end }}` подсвечены оранжевым
- [ ] Комментарии `--` подсвечены серым
- [ ] Ctrl+S сохраняет файл (Toast "Saved")
- [ ] После Ctrl+S dirty indicator (●) исчезает
- [ ] Ctrl+Shift+F форматирует SQL
- [ ] Ctrl+Space вызывает автодополнение
- [ ] Автодополнение предлагает имена параметров из /parameters/
- [ ] Автодополнение предлагает имена макросов из MacroRegistry
- [ ] Автодополнение предлагает значения `materialized:` (insert_fc, upsert_fc...)
- [ ] F12 на `{{ date_end }}` навигирует к parameters/date_end.yml

### FileTabs

- [ ] Открытые файлы отображаются как вкладки
- [ ] Несохранённый файл имеет индикатор ●
- [ ] Клик на вкладку переключает редактор на этот файл
- [ ] Закрытие вкладки с несохранёнными изменениями запрашивает подтверждение
- [ ] Breadcrumb показывает корректный путь к файлу
- [ ] Все сегменты breadcrumb кликабельны

### @config Инспектор

- [ ] PriorityChain: 5 уровней отображены (template, project, model, folder, SQL @config)
- [ ] Активный уровень выделен зелёным
- [ ] Перекрытые уровни: значение зачёркнуто серым
- [ ] Пустые уровни: отображаются как "—"
- [ ] PriorityChain обновляется при переключении между SQL-файлами
- [ ] ParametersUsed: все `{{ param }}` из текущего SQL перечислены
- [ ] ParametersUsed: для каждого параметра показаны domain_type и тип значения
- [ ] CteInspector: default materialization отображена
- [ ] CteInspector: by_context переопределения отображены
- [ ] GeneratedOutput: список форматов (dqcr, airflow, oracle_plsql, dbt)
- [ ] Кнопка Preview ▶ открывает просмотр сгенерированного файла

---

## ✓ Экран Validate

### Запуск и результаты

- [ ] Кнопка Validate в TopBar запускает валидацию текущей модели
- [ ] Индикатор загрузки отображается во время валидации
- [ ] Результаты появляются без полной перезагрузки страницы
- [ ] SummaryBar: счётчики passed/warning/error корректны
- [ ] Клик на счётчик фильтрует список (показывает только соответствующие)
- [ ] Кнопка "↻ Re-run" запускает валидацию повторно с теми же параметрами
- [ ] Выбор категорий правил (general, sql, descriptions, adb) работает
- [ ] Timestamp "Last run" обновляется после каждого запуска

### Категории и правила

- [ ] Каждая категория отображается как раскрывающаяся секция
- [ ] Заголовок категории: цветной индикатор + имя + счётчик "N/M"
- [ ] Зелёный индикатор: все правила прошли
- [ ] Жёлтый: есть warnings, нет errors
- [ ] Красный: есть errors
- [ ] Строка правила pass: зелёный ●, серый текст
- [ ] Строка правила warning: жёлтый ●, жёлтый bg, текст + кнопки
- [ ] Строка правила error: красный ●, красный bg, текст + кнопки

### Навигация и Quick Fix

- [ ] Кнопка "→ Open file" открывает проблемный файл в SQL Editor
- [ ] Курсор переходит на строку с ошибкой
- [ ] Кнопка "✦ Quick fix": патч `add_field` добавляет поле в model.yml
- [ ] После применения Quick Fix автоматически перезапускается валидация
- [ ] История запусков: последние 5 запусков отображены с временными метками

---

## ⊕ Экран Parameters

### Список и навигация

- [ ] Параметры разделены на GLOBAL и LOCAL секции
- [ ] Активный параметр подсвечен в списке
- [ ] Бейджи static/dynamic и domain_type видны в списке
- [ ] Клик на параметр открывает его редактор

### Редактор параметра

- [ ] Поля Name, Description, Domain Type, Scope загружены корректно
- [ ] Domain Type dropdown содержит все 8 типов из документации (string, number, date, datetime, bool, sql.condition, sql.expression, sql.identifier)
- [ ] Scope радиокнопки global / model работают
- [ ] ValuesTable: строки для всех контекстов параметра отображены
- [ ] Тип static: текстовое поле для значения
- [ ] Тип dynamic: Monaco SQL-редактор для подзапроса
- [ ] Кнопка "＋ Add context" добавляет новую строку
- [ ] Удаление строки контекста работает
- [ ] Кнопка "Test ▶" вызывает API и показывает результат выполнения
- [ ] ValuePreview: показывает resolved значение для активного контекста
- [ ] Сохранение параметра записывает файл на диск
- [ ] YAML-превью обновляется при изменении полей

---

## ⚙ Экран Build & Output

### Конфигурация

- [ ] EngineSelector: 4 движка (dqcr, airflow, dbt, oracle_plsql), один активен
- [ ] ContextSelector: список из contexts/ проекта
- [ ] DryRun чекбокс: при активации сборка не пишет файлы
- [ ] OutputPath: кастомный путь работает
- [ ] ModelSelector: можно выбрать одну или все модели

### Запуск и результаты

- [ ] Кнопка Build запускает сборку, появляется progress indicator
- [ ] CLI-команда транслируется в терминал во время сборки
- [ ] OutputTree: дерево файлов сборки появляется после завершения
- [ ] Клик на файл в дереве → просмотр содержимого (Monaco readonly)
- [ ] Кнопка Download: скачивает один файл
- [ ] Кнопка Download ZIP: скачивает архив со структурой target/
- [ ] BuildHistory: последние 10 сборок отображены
- [ ] Diff-view: кнопка появляется если есть предыдущая сборка

---

## 💻 Terminal Panel

- [ ] Terminal открывается в нижней панели
- [ ] xterm.js рендерится без артефактов
- [ ] Resize панели корректно подстраивает размер терминала
- [ ] WebSocket соединение устанавливается при открытии Terminal
- [ ] Команды передаются в PTY, вывод отображается в реальном времени
- [ ] CLI-трансляция: запуск Build → в терминале появляется команда generate
- [ ] CLI-трансляция: запуск Validate → в терминале появляется команда validate
- [ ] Цветной вывод ANSI поддерживается (зелёный ✓, красный ✗, жёлтый ⚠)
- [ ] Вкладки Terminal / Logs / Output переключаются
- [ ] Logs вкладка: structured log вывод Backend
- [ ] Output вкладка: список последних сгенерированных файлов
- [ ] Ctrl+L очищает терминал
- [ ] ↑/↓ навигация по истории команд

---

## 🧙 Project Creation Wizard

- [ ] Wizard открывается кнопкой "＋ New project" в TopBar
- [ ] StepIndicator показывает 4 шага, активный подсвечен
- [ ] Клик на прошедший шаг возвращает к нему
- [ ] **Шаг 1:** Поле Name: валидация (только латиница, цифры, _), длина 1–50
- [ ] **Шаг 1:** Description — необязательное, без ограничений
- [ ] **Шаг 1:** Карточки шаблонов (flx / dwh_mart / dq_control) кликабельны, выбранная подсвечена
- [ ] **Шаг 1:** Properties table: добавление/удаление произвольных свойств
- [ ] **Шаг 2:** default.yml создаётся автоматически
- [ ] **Шаг 2:** Кнопка "＋ Add context" открывает мини-форму
- [ ] **Шаг 3:** Имя модели валидируется (только латиница, цифры, _)
- [ ] **Шаг 3:** Атрибуты можно добавить через таблицу
- [ ] **Шаг 3:** Первая SQL-папка: имя и materialization обязательны
- [ ] **Шаг 4:** Превью директорий соответствует реальной структуре проекта
- [ ] **Шаг 4:** YAML project.yml корректен
- [ ] Кнопка "Создать проект" создаёт все файлы на диске
- [ ] После создания проект появляется в переключателе TopBar и открывается автоматически
- [ ] LivePreview: изменение Name на шаге 1 мгновенно обновляет дерево директорий

---

## 🔐 Аутентификация

- [ ] Неаутентифицированный запрос к /api/ → 401 Unauthorized
- [ ] Login форма (local mode) работает для корректных credentials
- [ ] Login форма: incorrect credentials → понятное сообщение об ошибке
- [ ] JWT токен сохраняется в httpOnly cookie или localStorage
- [ ] Просроченный токен → редирект на login
- [ ] Logout очищает токен и редиректит на login
- [ ] Роль `viewer`: кнопки Build и Save недоступны
- [ ] Роль `analyst`: YAML-режим в Model Editor недоступен
- [ ] Роль `admin`: раздел Administration виден в навигации
- [ ] Роль `engineer`: раздел Administration не виден

---

## 🎨 UX & Accessibility

### Внешний вид

- [ ] Light theme: все тексты читаемы, контраст ≥ 4.5:1
- [ ] Dark theme: все тексты читаемы, контраст ≥ 4.5:1
- [ ] Нет hardcoded цветов — только CSS variables
- [ ] Приложение корректно выглядит при 1280×800
- [ ] Приложение корректно выглядит при 1920×1080
- [ ] Горизонтальный scroll отсутствует при стандартных разрешениях

### Пустые состояния

- [ ] Нет открытых проектов → empty state с кнопкой "Create project"
- [ ] Нет результатов валидации → сообщение "Run validate to see results"
- [ ] Нет сборок → сообщение "No builds yet"
- [ ] Пустой список параметров → кнопка "＋ Add parameter"

### Загрузка и ошибки

- [ ] Все загрузки данных показывают skeleton/spinner
- [ ] Ошибка сети → toast с сообщением и кнопкой Retry
- [ ] 404 (файл не найден) → понятное сообщение, не белый экран
- [ ] 500 Backend error → toast с описанием, не crash приложения
- [ ] Все форматы дат человекочитаемы (не ISO timestamp)

### Keyboard navigation

- [ ] Tab-навигация проходит все интерактивные элементы в логичном порядке
- [ ] Enter/Space активирует кнопки и переключатели
- [ ] Escape закрывает модальные окна и dropdown
- [ ] Ctrl+P открывает QuickOpen файлов
- [ ] Ctrl+1..6 переключают вкладки

---

## 📊 Производительность

- [ ] Первый запуск (чистый кэш): LCP < 3 сек
- [ ] Повторный запуск: LCP < 1 сек (Service Worker или HTTP cache)
- [ ] Lineage граф 20 узлов отрисовывается < 500 мс
- [ ] Bidirectional sync задержка < 300 мс (измерить DevTools Performance)
- [ ] Валидация 10-модельного проекта завершается < 5 сек
- [ ] Сборка 10-модельного проекта завершается < 10 сек
- [ ] Прокрутка AttributesTable 100+ строк без лагов (>30 fps)
- [ ] Lighthouse Performance score > 80

---

## 🔒 Безопасность

- [ ] Path traversal: `GET /files/content?path=../../etc/passwd` → 400
- [ ] Только HTTPS на production стенде
- [ ] CORS не разрешает `*` — только конкретный домен
- [ ] Content-Security-Policy header установлен
- [ ] X-Frame-Options: DENY
- [ ] Зависимости без критических CVE (`npm audit` + `pip audit`)
- [ ] Secrets не логируются (env vars с `SECRET` в имени — masked)
- [ ] Terminal: произвольные команды кроме DQCR CLI заблокированы

---

## 📝 Release Checklist (перед деплоем)

- [ ] Все CRITICAL задачи текущей фазы закрыты
- [ ] Unit tests FE: pass rate 100%
- [ ] Integration tests BE: pass rate 100%
- [ ] E2E critical paths: pass rate 100%
- [ ] Docker image собирается без ошибок и warnings
- [ ] `docker compose up` → приложение работает без ручных действий
- [ ] README.md обновлён (install, configure, run)
- [ ] CHANGELOG.md обновлён
- [ ] Все secrets вынесены в env vars (нет hardcoded credentials)
- [ ] Lighthouse audit: Performance > 80, Accessibility > 90
- [ ] Проведено smoke-тестирование на staging стенде минимум одним Data Engineer
- [ ] Зарегистрированы known issues в Issues tracker

---

*Чеклист обновляется при добавлении новых функций.*  
*Ответственный за актуальность: Tech Lead проекта.*
